int largestAltitude(int* gain, int gainSize) {
    int i;
    int altitude = 0;
    int max = 0;   

    for (i = 0; i < gainSize; i++) {
        altitude += gain[i];
        if (altitude > max) {
            max = altitude;
        }
    }
    return max;
}
